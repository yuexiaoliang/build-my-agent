# 默认完全离线。不要把密钥粘贴到网页、聊天、公开运行记录或仓库。
# 只有 ALLOW_LIVE=1 且显式选择 live 才联网；没有自动重试。
ALLOW_LIVE=0
MODEL_BASE_URL=https://api.openai.com/v1
MODEL_NAME=
MODEL_API_KEY=
# 此适配器仅支持非流式 Chat Completions 文本/function tools。
# 必须先核对所选模型支持 max_completion_tokens 与工具协议。
MAX_COMPLETION_TOKENS=256
PORT=4317
