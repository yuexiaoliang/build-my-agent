# 从零重建说明

日期：2026-09-16。用户明确允许删除现有进度与产出，并要求按已确认方向重写仓库。

## 新基准

有前端经验的开发者，通过 Agent Harness 深化完整 AI 应用工程能力。AI 协助编码，学习者逐步主导解释、设计、诊断、验证与迁移；真实作品支撑可选公开表达。企业咨询、培训与独立产品保留为后续选择，不预设顾问职业，不用焦虑或就业保证推销课程。

## 替换范围

用新的目录树替换旧课程、旧 src/、旧 fixtures/、旧 scripts、旧 .learning/ 复习卡与状态、旧技能规则及依赖配置。不建立旧章号兼容层，也不把旧资料放进默认课程索引。新状态 S1.1、完成列表为空、学习证据为空。

Git 提交历史和已有标签不重写。本次重建的父提交是 `c6d1b26823b5b1862943cb988b05b2849ee1a904`。它仅用于回退，不作为当前教材；Git 历史中仍可能存在旧公开内容，工作树删除不是历史擦除。新重建在独立分支/PR 审阅后合并，不强推主分支。

## 实现范围

交付五阶段/二十单元的路线、S1 四课的可用教学材料、两张正式 PNG 与源图、简化导师和配图 Skill、全新离线 Web/CLI 实验台、单独结果检查与不可覆盖记录、自动测试与课程检查、重置的个人状态、低压力作品表达说明。

lab 是已准备的受控参考环境；harness 当前为空白作品区。S2–S5 保留具体目的、练习和验收规格，未提前实现生产级完整 Agent，也不标为 ready。没有真实学习发生，因此不新增个人能力证据。

## 取舍

先使用纯浏览器界面和 Node 内置 HTTP/测试模块，减少启动障碍；TypeScript 固定开发依赖单独类型检查。后续按项目需要比较框架，而不是要求从零手写所有外围设施。

图片实际产出，工具路径按环境区分；没有把 ChatGPT 图片工具或任意 API 假称为本仓库自带能力。参考实现只支持合成资料读取，没有给前期练习开放任意 Shell、文件写入或客户数据访问。

验收与限制见 [VALIDATION](VALIDATION.md)。修改范围很大，合并前应核对分支差异；不要只覆盖旧文件却保留所有旧入口并行生效。
