# 调整为本地从零学习的骨架

日期：2026-09-17。按用户本轮要求调整 PR #2，不自动合并。

## 修正范围

前一草稿 `4df50843b70edd939ad8a79a60f521d9f1967f3f` 已实现参考实验台并准备四课，误把“学习从零”理解为先看成品再做自己的版本。本次移除预制实验台、参考核心、Web/CLI、场景与测试、具体四课、两张 SVG 与图片清单、渲染脚本、依赖/锁文件/配置、运行 CI 和与该实现绑定的启动/验证说明。

两张 PNG 原本就未写入该草稿，现在其固定需求、源图与引用一起移除；不再把补交图片作为合并门槛。旧版运行测试和图片验证不适用于当前骨架，不继承其通过结论。

保留的是五阶段/二十单元的设计目标、依赖与验收，两个教学技能、当前小步空白备课模板、空的个人学习起点，以及可选作品表达。所有单元回到 planned、material=null；不创建实际课件、图片、代码、测试或运行数据。

当前小步由本地导师按真实环境和上下文先备课，课堂中 AI 增量编码、解释、验证，再按实际学习更新记录。具体产物不预制，不把责任变成学习者手敲代码或阅读资料包。

## 历史与交付

在原 PR 分支追加纠正提交，不强推、不重写历史、不改 main、不合并。PR 仍承接用户此前批准的全面重启：合并后旧课程和进度退出当前工作树；Git 历史仍保存旧版本，工作树删除不等于历史擦除。

此前对话的“完整重建包”、界面预览与验证包属于被撤回的预制方案，不再作为当前学习起点。需要本地使用时，以本次分支版本或更新后的骨架包为准，放入新目录，不与旧产物混放。

## 本次验证口径

仅针对骨架检查：JSON 可读、二十单元 ID/依赖与阶段一致且无环、全部 planned 且无材料、当前状态引用有效且无学习证据、两个 Skill 元数据可读、Markdown 本地链接存在、没有残留运行时代码/固定图片/旧启动入口。

本次没有运行应用、安装项目依赖、渲染图片或调用模型，因为这些已不属于当前交付。静态检查不证明本地环境适配、导师执行效果或学习者掌握。准备与实际学习发生时，才建立相应实现与验证。
