# 重建验证记录

日期：2026-09-16。这里记录工程检查，不是学习者成绩。

## 实际环境

Linux；Node 22.16.0（显式类型擦除）；TypeScript 5.8.3；@types/node 22.19.7；undici-types 6.21.0。运行时没有第三方依赖。类型检查使用本环境已有、版本相符的开发依赖，不冒充新从 npm 下载过。

本环境外网 DNS 不可用，未完成联网 `npm ci`；锁文件固定了依赖版本/来源，TypeScript tarball integrity 来自官方 npm 元数据，另两个类型包未写未核实的 integrity。CI 已配置 Node 22.16.0 与 24，但远端执行结果应另查，不能把配置文件当通过证明。

## 已执行与待验证

- 类型检查：覆盖 lab、harness 和 TypeScript tests；不把原生 TS 能执行当作类型检查。
- 离线自动测试：38 项通过（无失败、跳过或待实现测试）。覆盖协议结构、工具启用、整轮预算与重复组合、取消与期限、观察者隔离、固定任务检查、记录不覆盖、HTTP 输入与来源限制、服务端取消终态等。
- 课程检查：单元索引/依赖、当前状态、图片源/图哈希、实际 PNG 文件、Markdown 本地引用，以及自动生成路线的一致性。
- 图片：CairoSVG 2.8.2 在本机把两个原创 SVG 转成 PNG，并作单色压缩；已实际打开核对中文与边界，图片不是运行截图。
- 浏览器导航被当前环境策略阻止，未修改安全策略；不能声称完成真实浏览器到本地服务的端到端验证。隔离浏览器 UI 使用内存页面与合成 fetch，确认正常结果、取消反馈及 390px 无横向溢出，未出现 pageerror；1440px 与 390px 截图已目视检查。它不是端到端测试，HTTP 服务测试独立运行。
- 没有调用真实模型、收费图片服务或外部发布 API；可选 Chat Completions 适配器只用合成 transport 验证。
- 没有运行独立导师多轮模拟或真实学习试教；review 文件是规格。Windows/macOS、Node 24、本地凭证与真实提供商兼容性仍待各自验证。

## 可重跑入口

```sh
npm run verify
npm run demo
npm run learn
```

已有 PNG 直接使用，不需要安装渲染依赖。需要重新制作时由维护者配置可选 Python/CairoSVG，运行 `npm run render:visuals -- --publish` 后再检查图片和材料。

**边界：**单元 ready 仅表示当前教材、实验与资源已备好；程序通过不代表课程效果、模型能力、完整生产安全或学习者掌握。正式企业/公网交付前仍需认证、权限、配额、隔离、部署与实际场景验证。
