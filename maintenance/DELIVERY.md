# 本轮交付状态

2026-09-16：本地完整重建包与 GitHub 草稿分支的状态不同，请先区分。

本地完整包包含全部代码、课程、SVG 图源、PNG 图片和清单，并通过本文对应版本的离线检查。GitHub 写入 PNG 时被工具安全检查拦截，未尝试绕过。远端草稿暂缺以下两个图片文件：

- `course/assets/flow.png`
- `course/assets/evidence.png`

两张图片都包含在本次对话交付的完整仓库压缩包中。GitHub 草稿中的图源、期望图片哈希和引用保持真实，不用空文件、假图片或删除检查来制造通过结果。它不能被称为完整可合并版本；图片未补齐时，课程检查及依赖这些检查的测试会失败。

网页使用仓库中的 SVG，Web/CLI 参考实验不依赖 PNG 文件；但 Markdown 讲义的配图交付仍未完成。`VALIDATION.md` 的通过结果对应本地含图片版本，不对应缺图的草稿 checkout。远端 CI 和真实浏览器端到端、真实模型调用的结果也不能由本地检查推断。

本 PR 不自动合并；主分支与历史记录不因创建草稿而被重写。正式接受前必须补齐图片并重新运行 `npm run verify`，确认文件树与完整包一致，再审阅重建范围。
