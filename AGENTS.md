# Build My Agent 工作约定

本项目是面向中文开发者的 Agent Harness 课程。内容范围以 `docs/course-blueprint.md` 为准，能力验收见 `docs/competency-matrix.md`。教学执行以 `.agents/skills/agent-harness-tutor/SKILL.md` 为准；`docs/learning-method.md` 是面向学习者的说明。旧讲义中的“每个操作都预测”“短问答也必须提问”等要求不再生效，不叠加新旧流程。

## 先判断用户在做什么

- 开始、继续学习或复盘：读取教学 Skill、当前状态和当前相关材料，不默认加载或分发全部课程文档。
- 局部疑问：直接讲明白，必要时给例子；不强制测验、实现或记日志。
- 课程设计、基础设施、修文档或仓库维护：完成维护任务，不强行授课或等待通关，不提前实现后续 Agent 功能。
- 用户本次指令优先；教学检查不是执行授权。

## 教学最小约定

代码、测试和实验脚本由 AI 编写；学习者负责理解、验证并逐渐主导设计。不要求手敲、填 TODO 或默写。

**备课由导师承担，不转化为整章阅读作业。** 默认只呈现本步必要片段，说明它在完整任务中的位置。先示范已知条件下的推导，再邀请学习者处理小变体；不以未知提供商行为让新手猜答案。

提问前自然说明“为什么问、依据什么、回答到什么程度”。回答后指出具体依据和缺口；卡住时换例子或补前置，而不是无限追问。形式随内容和理解证据调整，不机械输出固定六段，也不要求每条命令先预测。

区分机制事实、可验证假设、设计取舍；区分计划、示意、离线运行、真实调用与学习者理解。测试通过不等于任务完成或学习者掌握。维护时不得因模板改变而重评历史记录。

## 仓库与内容

教材、Skill、模板、fixtures 与个人学习记录同库公开管理（2026-09-16 定位）。不写入密钥、完整聊天记录或无证据的能力评分。讲义保持中性，个人判断放 `.learning/`；公开示例使用合成内容并明确标注。

保留既有未提交修改，只提交本任务相关文件。课程维护优先采用可审阅分支/PR；不擅自合并、重置主分支或重打历史 tag。个人学习记录公开的现有范围不等于对其他仓库或额外私人信息的授权。

## Fixtures

录制按章节放 `fixtures/chNN/`，登记规则见 `fixtures/INDEX.md`。引用必须写完整相对路径。新增、移动或删除 fixture 要更新 INDEX 并通过 `npm run check:fixtures`。示意和手工变造不称为真实录制；回放具体能验证什么以当前执行路径为准。

## 学习快照与提交

保留现有学习者约定：每个小步结束提交一次，消息 `课程(NN.M): 主题`，包含相关讲义、fixtures、复习卡及状态；每章结束打 `chNN` tag。学习小步提交后自动推送远端及新 tag，不攒批（2026-09-14 约定）。

`.learning/` 自 2026-09-16 起随主仓库管理；旧私有远端 `yuexiaoliang/build-my-agent-learning` 冻结留档，不再写入。不改写历史提交；新证据纠正旧结论时追加注明日期的更正与证据，不抹去原判断。
