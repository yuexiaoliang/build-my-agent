# Build My Agent 工作约定

本项目是面向中文开发者的 Agent Harness 课程。课程范围以 `docs/course-blueprint.md` 为准，能力验收见 `docs/competency-matrix.md`。

## 区分授课与课程维护

- 用户要求开始学习、继续某课、讲解概念或复盘时，读取 `.agents/skills/agent-harness-tutor/SKILL.md`（教学原则见 `docs/teaching-principles.md`），采用其中的增量教学流程。
- 用户要求设计课程、准备基础设施、修文档或维护仓库时，完成该维护任务，不强行插入理解题或等待通关。维护期间不擅自提前实现学习主线的后续 Agent 功能。
- 用户当前指令优先于本项目的教学默认值；不要把学习检查变成重复确认或执行授权。

## 教学基本约定

代码、测试与实验脚本由 AI 编写。学习者负责逐步理解、预测、诊断和主导设计，不要求手敲代码、填 TODO 或代码默写。

每个学习小步围绕一个主要问题，解释改动及执行过程，展示实际验证证据。区分计划、模拟输出、真实运行与学习者理解，不能把其中一种当作另一种。

遇到理解疑问先解决当前问题。用户要求继续可以推进，但不能由此断定已经掌握。短概念问答不必机械走完整堂课流程。

## 公开内容与个人记录

教材、教学 Skill、匿名模板及教学回归案例可提交仓库。个人进度放在已忽略的 `.learning/` 中，按需读取与更新；不写入密钥、完整聊天记录或未经证据支持的能力评分。

不要将个人回答复制进公开教材。公开示例使用合成内容。提交时保留既有未提交修改，不顺手提交无关文件。

## 快照与提交约定

- 公开仓库按学习小步提交：每个小步结束提交一次，消息格式 `课程(NN.M): 主题`；每章结束打 tag `chNN`（如 `ch01`）。每次提交后自动推送到远端（含新 tag），不再攒批（2026-09-14 学习者约定）。
- `.learning/` 是独立私有仓库，远端 `yuexiaoliang/build-my-agent-learning`（private）；每个小步结束提交并推送，作为防丢备份，绝不进入公开仓库。
- 快照只包含当步相关内容；复习卡与章末汇总随小步提交到 `.learning/`，不改写历史。
- 公开版学习日志由章末汇总匿名化派生；个人判断、偏好和评价不进入公开仓库。
