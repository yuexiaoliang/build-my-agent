# 工作入口

本仓库服务有前端经验的中文学习者：以 Agent Harness 深化 AI 应用工程能力，以真实作品和持续表达增加职业选择。咨询、培训和产品是后续选项，不是现在的必修职业定位。

## 路由

- 开始/继续学习：先读 `.learning/state.json`、`course/catalog.json` 中当前单元、`.agents/skills/agent-harness-tutor/SKILL.md`，然后仅加载本课材料和必要代码。
- 制作或核对教学图片：按需读 `.agents/skills/teaching-visuals/SKILL.md`。客户端已确认能显示图片，不再询问；模型原生出图不是必要条件。
- 局部问题：直接回答，不附赠测试和状态写入。
- 维护/重构：做工程任务，不强行授课。`lab/` 的完成状态不等于学习者作品或理解完成。

## 不变量

保留中文与 TypeScript 主线。AI 可以编码，学习者逐步接管判断；不把手敲量、文档量、选项命中或“继续”当掌握证据。

真实运行、合成响应、机制示意、静态审查和学习证据分开报告。缺少提供商观测时不编造兼容性；图源、图片和客户端显示不能互相替代。

`course/catalog.json` 是单元状态和依赖的索引，`.learning/state.json` 是个人当前位置。不要在多个文档复制“当前下一步”。修改教材、图片或代码时同步相应证据/索引并运行 `npm run verify`。不能修改验收器来掩盖被测实现失败。

## 授权与资料

正常学习小步仅提交相关代码/讲义与简短证据，可沿用用户已要求的仓库提交与推送；大范围维护使用独立分支/PR。未经明确要求不自动合并，不强推或删除 Git 历史。课程路径从 S1.1 重启，但这不授权以后每次维护都清空进度。

不公开 `.env`、`.runs/`、客户资料、完整聊天或密钥。公开学习摘要只包含必要判断。新增付费批量调用、对外发布、客户数据上传、不可逆操作另行取得明确授权；工具访问并不等于业务授权。

新首课不需要真实模型。真实模式要由用户配置并启用；不得为了检查课程自动调用付费服务。系统/宿主指令及实际可用工具优先于仓库说明。
