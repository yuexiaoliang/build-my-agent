# 工作入口

目标：以已有前端经验为起点，通过 Agent Harness 深化完整 AI 应用工程能力；作品与专业表达支持长期选择。企业服务只是后续选项。

## 范围与路由

当前仓库只预设课程设计与教学规则。具体讲义、图片、代码、测试和运行证据在本地按学习小步生成。不得恢复预制整套实验台，或先完成核心 Harness 再让学习者阅读。

- 开始/继续学习：读取 `.learning/state.json`、`course/catalog.json` 当前项与 `.agents/skills/agent-harness-tutor/SKILL.md`；只加载直接需要的材料。
- 当前项为 planned 或 material 为 null：这是正常起点。导师先按当前上下文备这一步，再授课，不转成批量课程建设任务。
- 制作/修订教学图片：按需读取 `.agents/skills/teaching-visuals/SKILL.md`。客户端已确认能显示图片，不再询问。
- 局部问题：直接解释，必要时例子或图，不强制测验与记日志。
- 维护任务：只处理授权范围，不授课、不自动推进或清空个人学习记录。

## 信息归属

`course/catalog.json` 是单元目标、依赖与备课状态的唯一索引；`.learning/state.json` 是当前学习位置。路线页不复制完整索引，不维护另一份下一步。备课模板没有具体答案，也不要求原样打印。

教学节奏与反馈归导师技能；图片制作归配图技能；验收归 `course/assessment.md`。不为每次反馈在所有文件重复追加规则。代码/依赖尚未初始化时，不发送假想的安装、启动或测试命令。

## 资料与授权

AI 可以编码，学习者逐步主导判断。当前小步可以有必要的最小示范、合成输入和保护环境，但不得偷偷实现后续待学机制，或让外层保护代替被测代码通过验收。

不提交密钥、客户资料、完整聊天或私人运行记录。公开样例使用合成或已授权数据。真实调用、第三方上传、额外费用和不可逆操作须遵守已有授权，缺授权先处理，不把教学参与当授权。

当前重启已获用户同意，不代表以后可以自动重置。正常学习小步提交相关材料与简短证据，沿用用户已有的提交/推送约定；大范围维护走独立分支/PR。不擅自合并、强推、删除历史或丢弃本地未提交改动。系统/宿主指令和实际工具能力优先。
