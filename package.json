{
  "name": "build-my-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "start": "node --experimental-strip-types lab/server.ts",
    "demo": "node --experimental-strip-types lab/cli.ts",
    "learn": "node scripts/learn.mjs",
    "test": "node scripts/test.mjs",
    "check:types": "tsc --noEmit",
    "check:course": "node scripts/check-course.mjs",
    "verify": "npm run check:types && npm test && npm run check:course",
    "render:visuals": "node scripts/render-visuals.mjs"
  },
  "devDependencies": {
    "@types/node": "22.19.7",
    "typescript": "5.8.3"
  }
}
